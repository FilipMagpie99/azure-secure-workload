vnet-secure-workload            (CIDR: TODO — decyzja B)
│   segmentacja subnetowa wg D4a · jeden VNet, nie peering
│
├── snet-appgw                  /24  ← wymóg App Gateway WAF v2 (twardy)
│     [NSG: brak dedykowanego]
│     └─ App Gateway WAF .............................. Dzień 17
│
├── snet-app                    (CIDR: TODO)            ← BUDUJESZ DZIŚ
│     [NSG: nsg-app]  ──assoc──> snet-app
│     │   reguły nsg-app referują ASG (egzekucja gdy NIC-i wpięte)
│     ├─ asg-front  → NIC frontu  (pusty do Dnia 13)
│     ├─ asg-back   → NIC backendu (pusty do Dnia 13)
│     ├─ frontend (App Service) ..................... Dzień 13
│     └─ backend  (App Service) ..................... Dzień 13
│
├── snet-data                   (CIDR: TODO)            ← BUDUJESZ DZIŚ
│     [NSG: nsg-data]  ──assoc──> snet-data
│     └─ SQL private endpoint ....................... Dzień 15
│
└── snet-pe                     (CIDR: TODO — NIE /29!) ← BUDUJESZ DZIŚ
      [NSG: brak]
      ├─ KV private endpoint ......................... Dzień 16
      ├─ OpenAI private endpoint ..................... Dzień 18
      └─ backend private endpoint .................... Dzień 14

──────────────────────────────────────────────────────────────
DZIŚ (Dzień 2):  VNet · 4 subnety · nsg-app · nsg-data · asg-front · asg-back · asocjacje NSG→subnet
PÓŹNIEJ:         to, co siedzi w subnetach (compute / PE / WAF) — placeholdery powyżej
POZA v1:         firewall egress / NAT (backlog), kontrola wychodzącego ruchu

MODUŁ SIECI (Dzień 2) ── outputy ──┐
              snet-app id · snet-pe id           │
                                                 ▼
┌────────────────────── MODUŁ COMPUTE (Dni 3–4) ───────────────────────┐
│                                                                       │
│  INPUTY (bierze z sieci):                                             │
│    snet_app_id  → VNet integration (EGRESS)                           │
│    snet_pe_id   → private endpoint backendu (Dzień 4)                 │
│    location · resource_group_name                                     │
│                                                                       │
│  ┌───────────────────────── DZIEŃ 3 ─────────────────────────┐       │
│  │  App Service Plan   (SKU ≥ Standard: VNet int + PE)        │       │
│  │      ├── frontend (App Service)                            │       │
│  │      │     ├─ system-assigned MI ....... principal_id ●    │       │
│  │      │     └─ VNet integration → snet-app  (egress)        │       │
│  │      └── backend (App Service)                             │       │
│  │            ├─ system-assigned MI ....... principal_id ●    │       │
│  │            └─ VNet integration → snet-app  (egress)        │       │
│  └────────────────────────────────────────────────────────────┘      │
│                                                                       │
│  ┌───────────────────────── DZIEŃ 4 ─────────────────────────┐       │
│  │  backend INGRESS → szczelny (dwie warstwy):               │       │
│  │    ① private DNS zone  privatelink.azurewebsites.net      │       │
│  │         └─ link do VNet ............. „DNS prowadzi"       │       │
│  │    ② private endpoint (backend) → snet-pe                 │       │
│  │         └─ zone group → rekord A (auto)                   │       │
│  │    ③ public_network_access_enabled=false „egzekwuje"      │       │
│  └────────────────────────────────────────────────────────────┘      │
│                                                                       │
│  OUTPUTY (wystawia — TU JEST CAŁA WAGA):                             │
│    ● backend_mi_principal_id ──► SQL grant (D15) · KV role (D16)      │
│    ● frontend_mi_principal_id ─► (jeśli front czyta KV)              │
│      backend_name / app_id ───► App Gateway backend pool (D17)        │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
                              │
    outputy MI ───────────────┴──────────► konsumenci: SQL · KV · AI



    #dzień 4 zaaktualizowany


    MODUŁ SIECI (D2) ── outputy ──► snet-app id · snet-pe id · vnet id
  │  ZMIANA: snet-pe → private_endpoint_network_policies = "Enabled"
  │          (bez tego NSG nie widzi ruchu do PE = ślepa plamka detekcji)
  │  ZMIANA: filar 3 detekcji = VNET FLOW LOGS (nie NSG flow logs —
  │          retired dla nowych od 06.2025; tabela NTANetAnalytics)
  │
  ├────────► MODUŁ DNS (NOWY) ◄──── vnet id (referencja, nie string)
  │            strefa: privatelink.azurewebsites.net
  │            └─ vnet link ................. „DNS prowadzi"
  │            output: zone_id ──────────────┐
  │            (D15: privatelink.database.windows.net)
  │            (D16: privatelink.vaultcore.azure.net)
  ▼                                          │
┌──────────────── MODUŁ COMPUTE — DZIEŃ 4 ───┼──────────────────────┐
│  INPUTY: snet_pe_id · dns_zone_id ◄────────┘                      │
│                                                                    │
│  ① PRIVATE ENDPOINT (backend) → snet-pe                           │
│       └─ zone group → rekordy A: backend + backend.scm (auto)     │
│                                                                    │
│  ② INGRESS — wariant B (zamiast PNA=false):                       │
│       public_network_access_enabled = true   (świadomie)          │
│       ip_restriction_default_action = "Deny" (site: ZERO allow;   │
│                                        ruch tylko przez PE)       │
│       scm_ip_restriction        = [MOJE_IP]  (deploy, 1 host)     │
│       scm_ip_restriction_default_action = "Deny"                  │
│       scm_use_main_ip_restriction = false                         │
│       ryzyko: gwarancja konfiguracyjna, nie sieciowa              │
│       └─ kompensacja: Azure Policy audit na defaultAction (D-pol) │
│                                                                    │
│  ③ DIAGNOSTIC SETTING (backend) → Log Analytics   ◄── NOWE W D4   │
│       AppServiceIPSecAuditLogs  (każdy Deny = sygnał)             │
│       AppServiceHTTPLogs                                           │
│       „hardening produkuje obserwowalny sygnał od dnia 1"          │
│                                                                    │
│  ✔ DEFINITION OF DONE (3 testy):                                  │
│    [1] nslookup z Kudu frontendu → IP z snet-pe     (DNS)         │
│    [2] curl z zewnątrz → 403 (site i SCM spoza IP)  (Deny)        │
│    [3] frontend→backend 200 przez PE                              │
│        + Deny z [2] widoczny w AppServiceIPSecAuditLogs  (KQL)    │
└────────────────────────────────────────────────────────────────────┘


FILAR 1                    FILAR 2 (ingress)                 FILAR 3
  eskalacja uprawnień           odrzucone wejścia na app         naruszenia topologii

 ┌─────────────────────┐      ┌─────────────────────────┐      ┌──────────────────────┐
 │     Activity log     │      │   Diagnostic settings    │      │    VNet flow logs     │
 │  (subskrypcja,       │      │   na App Service         │      │  (Network Watcher +   │
 │   control plane)     │      │                          │      │   Traffic Analytics)  │
 └──────────┬──────────┘      └────────────┬────────────┘      └──────────┬───────────┘
            │                              │                              │
            │  export (diag setting        │  AppServiceIPSecAuditLogs    │  agregacja TA
            │  na scope subskrypcji)       │  AppServiceHTTPLogs          │  (10/60 min)
            ▼                              ▼                              ▼
 ┌─────────────────────────────────────────────────────────────────────────────────────┐
 │                        Log Analytics workspace  (law-secure-workload)               │
 │                                                                                     │
 │   ┌───────────────┐   ┌──────────────────────────┐   ┌────────────────────┐         │
 │   │ AzureActivity │   │ AppServiceIPSecAuditLogs │   │  NTANetAnalytics   │         │
 │   │               │   │ AppServiceHTTPLogs       │   │                    │         │
 │   └───────┬───────┘   └────────────┬─────────────┘   └─────────┬──────────┘         │
 │           │                        │                           │                    │
 │           └────────────────────────┼───────────────────────────┘                    │
 │                                    ▼                                                │
 │                    ┌──────────────────────────────┐                                 │
 │                    │  Detekcje KQL (queries/)      │                                 │
 │                    │  scheduled query alerts       │                                 │
 │                    │  Sentinel-ready (backlog)     │                                 │
 │                    └──────────────────────────────┘                                 │
 └─────────────────────────────────────────────────────────────────────────────────────┘

  ── Filar 2 (tożsamość / Key Vault → AzureDiagnostics) dojdzie w dniu KV,
     podpinając diag setting do TEGO SAMEGO workspace. Dzień 5 instrumentuje to, co stoi.